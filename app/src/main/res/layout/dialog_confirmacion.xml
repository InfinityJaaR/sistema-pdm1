<?xml version="1.0" encoding="utf-8"?>
<!-- Mantenemos el ScrollView por si el texto de confirmación es muy largo -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- TÍTULO DEL DIÁLOGO -->

        <!-- TEXTO DE PREGUNTA -->
        <TextView
            android:id="@+id/tv_confirmacion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:gravity="center"
            android:text="@string/modal_titulo_confirmacion"
            android:textColor="#12122A"
            android:textSize="18sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/confirmacion_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/msg_confirmacion_pregunta"
            android:textColor="#555555"
            android:textSize="16sp"
            android:gravity="center"
            android:layout_marginBottom="24dp" />

        <!-- CONTENEDOR DE BOTONES -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- BOTÓN NO / DECLINAR -->
            <Button
                android:id="@+id/btn_no"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="52dp"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:text="@string/no"
                android:textColor="#12122A"
                app:strokeColor="#12122A"
                app:cornerRadius="8dp" />

            <!-- BOTÓN SÍ / CONFIRMAR -->
            <Button
                android:id="@+id/btn_si"
                android:layout_width="0dp"
                android:layout_height="52dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="@string/yes"
                android:backgroundTint="#12122A"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                app:cornerRadius="8dp" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>